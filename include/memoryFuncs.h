int** append(int**, int*, int*);

int** appendValueCopy(int**, int*, int*, int);

int* appendNum(int*, int*, int);

char** addMemStr(char**, int*);

char** appendStrCopy(char**, int*, char*);

char** appendStr(char**, int*, char*);

char* appendChar(char*, char);